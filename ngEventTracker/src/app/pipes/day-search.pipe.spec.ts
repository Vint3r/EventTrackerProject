import { DaySearchPipe } from './day-search.pipe';

describe('DaySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DaySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
