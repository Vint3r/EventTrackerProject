import { CatSearchPipe } from './cat-search.pipe';

describe('CatSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CatSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
