import { SwitchedSelectedPipe } from './switched-selected.pipe';

describe('SwitchedSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchedSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
