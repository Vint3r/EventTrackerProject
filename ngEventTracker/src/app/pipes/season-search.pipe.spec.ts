import { SeasonSearchPipe } from './season-search.pipe';

describe('SeasonSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
